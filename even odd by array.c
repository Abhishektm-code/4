#include <stdio.h>

int main()
{
    int a[50],size=0,i;
    printf("enter the size",size);
    scanf("%d",&size);

    printf("enter  the number:",&a[i]);
    {
    for(i=0; i<=size; i++)
    {
        {
        scanf("%d",&a[i]);
         }
        if(a[i]%2==0)
        {
        printf("even number %d\n",a[i]);
        }
        else
        {

        printf("odd number %d\n",a[i]);
        }
    }
    }
}

